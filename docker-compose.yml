version: "3"
services:
    bot:
        build: .
        restart: unless-stopped
        depends_on:
            - mysql
        links:
            - mysql
        networks:
            - backend
        env_file:
            - .env
    mysql:
        image: mysql:8
        restart: always
        ports:
            # <Port exposed> : < Port inside container>
            - '3306:3306'
        expose:
            # Opens port 3306 on the container
            - '3306'
        volumes:
            - db:/data/ploxy
        env_file: 
            - .env
        networks:
            - backend
networks:
    backend:
        driver: bridge

volumes:
    db: {}
